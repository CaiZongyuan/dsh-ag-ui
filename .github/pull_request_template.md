## Summary

- Describe the change.

## Behavior and compatibility

- Describe compatibility and behavior changes.

## Verification

- [ ] `pnpm -r --workspace-concurrency=1 --include-workspace-root check`
- [ ] `pnpm -r --workspace-concurrency=1 --include-workspace-root pack --dry-run`

Commands run:

```text

```

## Documentation

- [ ] `README.md` and `README.zh.md` remain equivalent when public behavior changed.
- [ ] `CHANGELOG.md` is updated for a release-facing change.
